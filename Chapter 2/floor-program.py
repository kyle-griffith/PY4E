# Create a program for determining the current floor you are on if you were in Europe

# Declaring the variable that holds our initial input / prompt
inp = input('Europe floor? ')
# Declaring the US variable, and converting the string recieved from the input / prompt into an integer to interact with our + 1
usf = int(inp) + 1
# Returning the information recieved from the input
print('US floor', usf)